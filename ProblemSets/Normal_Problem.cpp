#include <bits/stdc++.h>
using namespace std;
void swap(string a,string  b)
{
    if(a[i]=)
}

int main()
{
    int length;
    string a, b;
    cin >> a;
    for (int i = 0; i <= length; i++)
    {
        if (a[i] == 'p')
        {
            a[i] == 'q';
            b = a;
            cout << b;
        }

        cout << 0;
        break;
    }
}